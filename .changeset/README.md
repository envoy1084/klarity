# Changesets

Run `pnpm changeset` for every user-visible change. Choose the SemVer impact and explain the
consumer-facing change. The release workflow turns accumulated changesets into a version PR; merging
that PR publishes the package and creates a GitHub release.
